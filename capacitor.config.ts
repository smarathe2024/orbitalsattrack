import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.orbital.tracker',
  appName: 'Orbital Satellite Tracker',
  webDir: 'dist'
};

export default config;
